application:
  name: "Books Parser"
  version: "1.0.0"
  environment: "development"

scraper:
  base_url: "https://books.toscrape.com"
  delay: 1
  timeout: 30

output:
  format: "csv"
  directory: "output"

logging:
  level: "info"
  file: "logs/application.log"

